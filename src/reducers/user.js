import {} from 'src/actions';

const initialState = {
  login: 'totoDu93',
};

const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
